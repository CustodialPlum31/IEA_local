<%- include("partials/_header") %>

<div class="container mt-5">
  <div class="row">
    <div class="col-md-7">
        <table class="table table-striped table-primary">
            <thead>
              <tr>
                <td>N°</td>
                <td>Nombre</td>
                <td>Descripcion</td>
                <td>No.serie </td>
              </tr>
            </thead>
            <tbody>
                <% if (data) { %> 
                  <% for(var i = 0; i < data.length; i++) {
                %>
                <tr>
                  <td><%= (i + 1) %></td>
                  <td><%= data[i].nombre %></td>
                  <td><%= data[i].descripcion %></td>
                  <td><%= data[i].No_serie %></td>
                  <td>
                    <a href="/equipo/delete/<%= data[i].id_e %>" class="btn btn-danger">Borrar</a>
                    <a href="/equipo/update/<%= data[i].id_e %>" class="btn btn-info">Actualizar</a>
                  </td>
                </tr>
                <% } %> <% } %>
            </tbody>
            
          </table>



    </div>
    <div class="col-md-5">
        <div class="card">
            <div class="card-body">

                <form action="/equipo/add" method="POST">
                    <input
                      type="text"
                      name="nombre"
                      placeholder="Nombre"
                      class="form-control mb-2"
                      autofocus
                    />
                    <input
                      type="text"
                      name="descripcion"
                      placeholder="Descripcion"
                      class="form-control mb-2"
                    />
                    <input
                      type="text"
                      name="No_serie"
                      placeholder="No.serie"
                      class="form-control mb-2"
                    />
                    <button type="submit" class="btn btn-info">Agregar equipo</button>
                  </form>

       </div>
      </div>
    </div>
  </div>
</div>

<%- include("partials/_footer") %>