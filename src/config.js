// config.js
const PORT = process.env.PORT || 3000;
const DB_HOST =  process.env.PORT ||'localhost';
const DB_USER = process.env.PORT || 'root';
const DB_PASSWORD = process.env.PORT || 'Up1_M4N12#';
const DB_NAME = process.env.PORT || 'ieamysql';
const DB_PORT = process.env.PORT || 3306;

module.exports = {
    PORT,
    DB_HOST,
    DB_USER,
    DB_PASSWORD,
    DB_NAME,
    DB_PORT
};
